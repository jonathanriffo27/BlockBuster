<%= form_with(model: movie) do |form| %>
  <% if movie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>
      <ul>
        <% movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :name, 'Nombre', style: "display: block", class:'form-label text-light' %>
    <%= form.text_field :name, class:'form-control w-25' %>
  </div>
  <div>
    <%= form.label :client_id, 'Cliente', style: "display: block", class:'form-label text-light mt-2' %>
    <%= form.select :client_id, options_for_select(@clients),{}, { class: 'form-select mb-2 w-25' } %>
  </div>
  <div>
    <%= form.submit 'Aceptar', class: "btn btn-secondary mt-3" %>
  </div>
<% end %>
